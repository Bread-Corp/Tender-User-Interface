name: Build - React App - staging

on:
  workflow_dispatch:           # Allow manual triggering from Actions tab
  push:
    branches:
      - staging                   # Also run automatically on merge to staging

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      NODE_VERSION: '22.16.0'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: npm install
        working-directory: ./Tender-UserInterface/Tender-User-Interface-React

      - name: Build React app
        run: npm run build
        working-directory: ./Tender-UserInterface/Tender-User-Interface-React

      # Uncomment below to run React tests
      # - name: Run React tests
      #   run: npm test -- --watchAll=false
      #   working-directory: ./Tender-UserInterface/Tender-User-Interface-React
